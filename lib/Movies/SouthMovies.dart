import 'package:flutter/material.dart';
class SouthMOviesPage extends StatelessWidget {
  const SouthMOviesPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text("South Movies"),
      ),
    );
  }
}