import 'package:flutter/material.dart';
class WebSeries extends StatelessWidget {
  const WebSeries({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text("WebSeries"),
      ),
    );
  }
}